# `auth_type` in the IR — implementation plan

Plan readiness: ready.

## Outcome and scope

Add an optional provider-level `auth_type` to the IR that selects how the
credential is presented to the upstream, and document which upstream uses
which mechanism.

- Values: `official` (default), `bearer`, `x-api-key`, `none`.
- Requested because the pi-derived targets (pi, prime-agent) reject
  `Authorization: Bearer ENV:NAME` with no alternative, so a gateway that
  wants bearer auth cannot be generated at all (reported "连不上").
- Exclusions: MCP-server auth is unchanged (`Bearer ENV:` keeps working
  there). No new `auth_type` mapping for targets outside the pi family; they
  must report a diagnostic instead of ignoring it. Not fixing the separate
  `contextWindow: 0` / `maxTokens: 0` sentinels in the pi emitter.

Existing changes: none (tree clean at `3db170e`).
Authorization: implementation — user chose the value set and asked for the docs.

## Design and constraints

Verified upstream facts (source, not docs):

- pi `packages/coding-agent/src/core/provider-composer.ts:274-286` —
  `authHeader: true` adds `Authorization: Bearer <resolved apiKey>` and throws
  `authHeader requires a resolved API key` when the key is unresolved.
- pi `packages/ai/src/api/openai-completions.ts:787` — `new OpenAI({ apiKey })`,
  so the OpenAI transports default to `Authorization: Bearer`.
- pi `packages/ai/src/api/anthropic-messages.ts:979-981` — the normal path is
  `new Anthropic({ apiKey, authToken: null })` → `x-api-key`. Bearer is used
  only for `provider === "github-copilot"` and OAuth `sk-ant-oat` tokens.
- omp `models-config-schema-bundle.ts` — provider carries `auth:
  "apiKey" | "none" | "oauth"` + `authHeader: boolean`, and rejects
  `apiKey: ""` (`mustBe("apiKey a non-empty string")`). prime-agent is a pi
  fork with the same `authHeader` schema and Bearer injection
  (`model-registry.ts:202`, `:1561`).

Semantics: `auth_type` is orthogonal to `protocol`; it overrides the
credential-injection default of the selected protocol. `official` means
"target/protocol native" and is a no-op, so existing configs keep working.

IR contract and invariants:

- `auth_type` is parsed with `KnownFields(true)`; an unknown value is an error.
- `bearer` / `x-api-key` require `api_key` to be present (`Value` or `FromEnv`).
- `none` forbids `api_key`.
- `official` adds no constraint.
- A target that does not implement the mapping must emit a diagnostic for any
  non-`official` value — the emitter contract forbids silently dropping meaning.

Target mapping (pi family):

| `auth_type` | pi / prime-agent |
| --- | --- |
| `official` | native `apiKey` as today |
| `bearer` | native `apiKey` + `authHeader: true` |
| `x-api-key` | accepted only for `protocol: anthropic-messages` (already the native default); diagnostic on the OpenAI protocols, whose transport always authenticates with Bearer |
| `none` | omit the native `apiKey` field entirely |

Omitting `apiKey` also removes the current `apiKey: ""` emission, which omp
rejects at schema validation and pi turns into `No API key for provider`.

Capability plumbing: `target.AuthTypeValidator` is an optional interface.
Targets implementing it own representability; the default path (all other
targets) diagnoses every non-`official` value. The check is invoked from
`app.runTargetValidation` so `validate` and `gen` agree.

Upstream reference table (docs deliverable; pi/OpenAI rows are source-verified,
gateway rows are convention):

| Upstream | Protocol | Native auth | `auth_type` |
| --- | --- | --- | --- |
| Anthropic official | `anthropic-messages` | `x-api-key` | `official` |
| OpenAI official | `openai-completions` / `openai-responses` | `Authorization: Bearer` | `official` |
| OpenRouter | `openai-completions` | `Authorization: Bearer` | `official` |
| new-api / one-api (fronting Claude) | `anthropic-messages` | gateway wants Bearer | `bearer` |
| new-api / one-api (fronting GPT) | `openai-completions` | `Authorization: Bearer` | `official` |
| LiteLLM / Vercel AI Gateway | `openai-completions` | `Authorization: Bearer` | `official` |
| Ollama / LM Studio / llama.cpp / vLLM | any | no credential | `none` |

## Acceptance map

| ID | Behavior or invariant | Task | Check and expected result |
| --- | --- | --- | --- |
| A1 | `auth_type: bearer` + `api_key: ENV:T` → pi emits `authHeader: true` and `apiKey: "$T"`; prime-agent emits `"authHeader": true` and `"apiKey": "T"` | T2 | unit test + `gen` on a probe config shows both fields |
| A2 | `auth_type: none` omits `apiKey` (never `""`) | T2 | unit test + `gen` output has no `apiKey` key |
| A3 | `bearer`/`x-api-key` without `api_key` → IR error; `none` with `api_key` → IR error | T1 | `ir.Load` diagnostic table rows |
| A4 | unknown `auth_type` value → diagnostic (matches `protocol` handling) | T1 | `ir.Load` diagnostic table row |
| A5 | `x-api-key` + `openai-completions` → diagnostic; + `anthropic-messages` → accepted | T2 | unit test on both protocols |
| A6 | non-`official` `auth_type` on a target with no mapping (e.g. `crush`) → diagnostic naming the target | T2 | shared test in `internal/target/all` |
| A7 | `official` (and absent) is a no-op everywhere | T2 | existing golden/unit tests stay green; `make check` |

## Execution

Read this plan and linked spec; use counterweight's `references/deep.md`
execution loop. One implementation task at a time, run checks, inspect the
diff, commit, then continue.

### T1: IR field, default, and structural validation

Status: done
Depends on: none
Acceptance: A3, A4
Targets: `internal/ir/types.go` (new `AuthType` type + `Provider.AuthType`),
`internal/ir/load.go` (`Validate`), `internal/ir/load_test.go`
Contracts: `ir.AuthTypeOfficial|Bearer|XAPIKey|None`; empty value means
`official`

- [x] Add the `AuthType` enum with the four string values and the provider
      field; keep `official` as the zero-value meaning.
- [x] Validate: unknown value; `bearer`/`x-api-key` require `api_key`;
      `none` forbids it.
- [x] Verify at repo root: `go test ./internal/ir/` — new table rows pass and
      the existing cases stay green.
- [x] Inspect the diff for scope.
- [x] Commit the IR slice.

Evidence: `go test ./internal/ir/` → ok (3.032s); 4 new rows in
`TestValidateFindings`.

### T2: pi-family mapping plus the no-silent-drop guard

Status: pending
Depends on: T1
Acceptance: A1, A2, A5, A6, A7
Targets: `internal/target/target.go` (optional interface + default
diagnostics helper), `internal/app/app.go` (call site), `internal/target/pi/pi.go`,
`internal/target/primeagent/primeagent.go`, their tests, and
`internal/target/all/scalar_test.go`
Contracts: `target.AuthTypeValidator`; a target that does not implement it
reports `auth_type: <v> is not implemented` for every non-`official` value

- [ ] pi: map `bearer` → `authHeader: true`, `none` → omit `apiKey`,
      `x-api-key` → accept on `anthropic-messages` only; stop emitting
      `apiKey: ""`. Retarget the `Bearer ENV:` rejection message at
      `auth_type: bearer`.
- [ ] prime-agent: same mapping using its provider `authHeader` field.
- [ ] Emit the default "not implemented" diagnostic from the shared helper so
      the other targets never silently ignore `auth_type`.
- [ ] Verify at repo root: `go test ./...` and a `gen` run on a bearer probe
      config; expect `authHeader: true` present and no `apiKey: ""` anywhere.
- [ ] Inspect the diff for scope and contract mistakes.
- [ ] Commit the mapping and guard slice.

### T3: Document `auth_type` and the upstream table

Status: pending
Depends on: T2
Acceptance: A1 (documented behavior matches the emitted output)
Targets: `docs/protocol.md` (Provider section, IR validation list),
`docs/agents/pi.md`, `docs/agents/prime-agent.md`
Contracts: the documented value semantics equal T1/T2 behavior

- [ ] Document the field, its values, the default, the validation rules, and
      the upstream→`auth_type` table; state that `auth_type` overrides the
      protocol default and is orthogonal to `protocol`.
- [ ] Update the pi/prime-agent pages: `auth_type: bearer` replaces the old
      "Bearer ENV: is not representable" dead end.
- [ ] Verify: re-read the docs against the emitted artifacts from T2; run
      `make check`.
- [ ] Commit the docs slice.

## Final acceptance

`gen --config <bearer probe> -t pi,prime-agent` prints `authHeader: true` for
each provider and no empty `apiKey`; `validate -t crush` on the same IR reports
the not-implemented diagnostic; `make check` passes.

## Progress and handoff

None yet.
